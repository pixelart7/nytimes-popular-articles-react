## How to run

1. Install dependencies using: `npm install` or `yarn install`
2. Run the local server using `npm start` or `yarn start`
3. Visit the page using the link shows in the terminal, usually `http://localhost:3000` unless the port is in used
